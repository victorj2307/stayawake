StayAwake **{{TAG}}** — Windows x64 portable build.

## Install
1. Download `{{ZIP_NAME}}` from this release.
2. Extract and run `StayAwake.exe` (no installer; `settings.json` is created beside the EXE).

## Requirements
- Windows 10 or later
- No administrator rights required

{{CHANGELOG_LINE}}
